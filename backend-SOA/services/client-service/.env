    # CRM Service env example
# Copy file này thành `.env` trong cùng thư mục trước khi chạy service.
#
# Lưu ý Windows: `localhost` đôi khi resolve sang IPv6 (::1) trong khi Mongo chỉ listen IPv4
# => dùng 127.0.0.1 để tránh ECONNREFUSED ::1:27017.
# CRM Service env example
# Copy file này thành `.env` trong cùng thư mục trước khi chạy service.

PORT=5007
MONGO_URL=mongodb://localhost:27017/hrm-client

# JWT_SECRET nên giống identity-service để verify token OK.
JWT_SECRET=dev_secret


